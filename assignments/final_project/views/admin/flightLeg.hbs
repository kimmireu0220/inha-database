<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header><h1>AIRLINE Reservation System</h1><nav><a href="/">홈</a><a href="/admin">관리자</a><a href="/user">사용자</a></nav></header>
    <main>
        <h2>항공편 구간 관리</h2>
        <h3>구간 추가</h3>
        <form method="post" action="/admin/flight-leg">
            <div><label>항공편: <select name="Flight_number" required>{{#each flights}}<option value="{{Flight_number}}">{{Flight_number}}</option>{{/each}}</select></label></div>
            <div><label>구간 번호: <input type="number" name="Leg_number" required></label></div>
            <div><label>출발 공항: <select name="Departure_airport_code" required>{{#each airports}}<option value="{{Airport_code}}">{{Name}} ({{Airport_code}})</option>{{/each}}</select></label></div>
            <div><label>출발 시간: <input type="time" name="Scheduled_departure_time" required></label></div>
            <div><label>도착 공항: <select name="Arrival_airport_code" required>{{#each airports}}<option value="{{Airport_code}}">{{Name}} ({{Airport_code}})</option>{{/each}}</select></label></div>
            <div><label>도착 시간: <input type="time" name="Scheduled_arrival_time" required></label></div>
            <div><button type="submit">추가</button></div>
        </form>
        <h3>구간 목록</h3>
        <table><thead><tr><th>항공편</th><th>구간</th><th>출발지</th><th>출발시간</th><th>도착지</th><th>도착시간</th><th>작업</th></tr></thead>
        <tbody>{{#each flightLegs}}<tr><td>{{Flight_number}}</td><td>{{Leg_number}}</td><td>{{Departure_airport_name}} ({{Departure_airport_code}})</td><td>{{Scheduled_departure_time}}</td><td>{{Arrival_airport_name}} ({{Arrival_airport_code}})</td><td>{{Scheduled_arrival_time}}</td>
        <td><a href="/admin/flight-leg/update/{{Flight_number}}/{{Leg_number}}">수정</a> <form method="post" action="/admin/flight-leg/delete" style="display:inline;"><input type="hidden" name="flightNumber" value="{{Flight_number}}"><input type="hidden" name="legNumber" value="{{Leg_number}}"><button type="submit">삭제</button></form></td></tr>{{/each}}</tbody></table>
        <a href="/admin">관리자 메인</a>
    </main>
</body>
</html>

